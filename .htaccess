RewriteEngine On

RewriteCond %{REQUEST_FILENAME} !-f

# Rewrite for index.html
RewriteRule ^(.*) index.html?wild=$1 [NC,L]
RewriteCond %{HTTP_HOST} ^(.+?)\.$
RewriteRule ^ http://%1%{REQUEST_URI} [L,R=302,NE]
